export interface FactoryEfficiency {
    equipmentStatus: number; // 0 to 1, where 1 is perfect condition 
    workerSkill: number; //  0 to 1, where 1 represents maximum skill
    temperature: number; // Example: degrees Celsius, with an optimal range
}
